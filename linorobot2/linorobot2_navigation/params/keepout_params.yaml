costmap_filter_info_server:
  ros__parameters:
    use_sim_time: true
    type: 0
    # filter_info_topic: /costmap_filter_info
    # mask_topic: <keepout_filter_mask>
    filter_info_topic: costmap_filter_info
    mask_topic: keepout_filter_mask
    base: 0.8
    multiplier: 1.0
