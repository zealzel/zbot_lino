### On raspberrypi, download ros-develop and installation script

```bash
cd $HOME
git clone https://github.com/zealzel/ros-develop
cd "$HOME/ros-develop/setup_raspberry/zbot_lino"
./install_lino.sh
```
